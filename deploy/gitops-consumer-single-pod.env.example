# GitOps / K8s: 기존 consumer-pcc, consumer-embed, consumer-upsert Deployment를
# consumer 하나로 합친 뒤, 동일 이미지에 아래만 맞추면 된다.
#
# SPRING_PROFILES_ACTIVE: 운영용 consumer 프로필 (예: dev-consumer,prod-consumer)
# rag.worker.group / RAG_WORKER_GROUP: 제거됨 — 단일 ConsumerPipelineRunner만 사용
